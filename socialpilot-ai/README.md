# 🚀 SocialPilot AI

AI-генератор контента для социальных сетей с авторизацией и платными подписками через Stripe.

![Next.js](https://img.shields.io/badge/Next.js-14-black?style=flat-square&logo=next.js)
![TypeScript](https://img.shields.io/badge/TypeScript-5-blue?style=flat-square&logo=typescript)
![Prisma](https://img.shields.io/badge/Prisma-5-2D3748?style=flat-square&logo=prisma)
![Stripe](https://img.shields.io/badge/Stripe-Ready-635BFF?style=flat-square&logo=stripe)

## ✨ Функционал

### Для пользователей
- 🎯 **Генерация постов** для Twitter, LinkedIn, Instagram
- 📝 **4 типа контента**: посты, threads, списки, вопросы
- 🎨 **4 тона голоса**: профессиональный, разговорный, юмористический, вдохновляющий
- 👤 **Личный кабинет** с историей генераций
- 📊 **Статистика использования** и лимиты

### Технические
- 🔐 **Авторизация** через NextAuth.js (email/пароль + OAuth)
- 💳 **Stripe** для платных подписок
- 📦 **Prisma + SQLite** база данных
- ⚡ **Демо-режим** работает без API ключей

## 🚀 Быстрый старт

### 1. Установка

```bash
cd socialpilot-ai
npm install
```

### 2. Настройка окружения

Скопируйте `.env.example` в `.env` и заполните переменные:

```bash
cp .env.example .env
```

**Минимальная конфигурация (для разработки):**
```env
DATABASE_URL="file:./dev.db"
AUTH_SECRET="your-secret-key-32-characters"
AUTH_URL="http://localhost:3000"
```

### 3. Инициализация базы данных

```bash
npx prisma generate
npx prisma db push
```

### 4. Запуск

```bash
npm run dev
```

Приложение: http://localhost:3000

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание | Обязательно |
|------------|----------|-------------|
| `DATABASE_URL` | URL базы данных (SQLite по умолчанию) | ✅ |
| `AUTH_SECRET` | Секретный ключ для NextAuth | ✅ |
| `AUTH_URL` | URL приложения | ✅ |
| `OPENAI_API_KEY` | API ключ OpenAI (для реальной генерации) | ❌ |
| `STRIPE_SECRET_KEY` | Секретный ключ Stripe | ❌ |
| `STRIPE_WEBHOOK_SECRET` | Секрет для Stripe webhooks | ❌ |
| `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` | Публичный ключ Stripe | ❌ |
| `STRIPE_PRO_PRICE_ID` | ID цены для Pro тарифа | ❌ |
| `STRIPE_UNLIMITED_PRICE_ID` | ID цены для Unlimited тарифа | ❌ |
| `AUTH_GOOGLE_ID` | Google OAuth Client ID | ❌ |
| `AUTH_GOOGLE_SECRET` | Google OAuth Secret | ❌ |

### Режимы работы

**Без API ключей (демо):**
- Авторизация работает
- Генерация использует заготовленные ответы
- Stripe недоступен (кнопки показывают сообщение об ошибке)

**С OpenAI API:**
- Добавьте `OPENAI_API_KEY` для реальной AI генерации

**С Stripe:**
1. Создайте аккаунт на [stripe.com](https://stripe.com)
2. Создайте продукты и цены в Stripe Dashboard
3. Добавьте ключи в `.env`
4. Настройте webhook на `/api/stripe/webhook`

## 📁 Структура проекта

```
src/
├── app/
│   ├── (auth)/           # Страницы авторизации
│   │   ├── login/
│   │   └── register/
│   ├── api/
│   │   ├── auth/         # NextAuth API
│   │   ├── generate/     # Генерация контента
│   │   └── stripe/       # Stripe endpoints
│   ├── dashboard/        # Личный кабинет
│   ├── generate/         # Генератор контента
│   ├── pricing/          # Страница с ценами
│   └── page.tsx          # Лендинг
├── components/           # UI компоненты
├── lib/
│   ├── auth.ts          # Конфигурация NextAuth
│   ├── prisma.ts        # Prisma клиент
│   └── stripe.ts        # Stripe клиент
└── middleware.ts        # Защита роутов
```

## 💰 Тарифы

| Тариф | Цена | Лимит генераций |
|-------|------|-----------------|
| Free | 0₽ | 5/месяц |
| Pro | 990₽/мес | 100/месяц |
| Unlimited | 2490₽/мес | ∞ |

## 🔧 Stripe настройка

### 1. Создание продуктов

В Stripe Dashboard создайте два продукта:
- **Pro** — 990₽/месяц (recurring)
- **Unlimited** — 2490₽/месяц (recurring)

### 2. Получение Price IDs

После создания продуктов скопируйте Price IDs и добавьте в `.env`:
```env
STRIPE_PRO_PRICE_ID=price_xxx
STRIPE_UNLIMITED_PRICE_ID=price_yyy
```

### 3. Настройка Webhook

В Stripe Dashboard → Developers → Webhooks:
1. Добавьте endpoint: `https://your-domain.com/api/stripe/webhook`
2. Выберите события:
   - `checkout.session.completed`
   - `invoice.payment_succeeded`
   - `customer.subscription.updated`
   - `customer.subscription.deleted`
3. Скопируйте Signing Secret в `STRIPE_WEBHOOK_SECRET`

### 4. Тестирование

Используйте тестовые ключи Stripe и карту `4242 4242 4242 4242`.

## 🌐 Деплой

### Vercel (рекомендуется)

1. Push код на GitHub
2. Импортируйте в Vercel
3. Добавьте переменные окружения
4. Deploy!

**Важно для production:**
- Замените SQLite на PostgreSQL
- Настройте реальные Stripe ключи
- Добавьте реальный `AUTH_SECRET`

### База данных для production

Замените SQLite на PostgreSQL (Supabase, Neon, Railway):

```prisma
// prisma/schema.prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

## 📈 Путь к $1000/месяц

| Тариф | Нужно клиентов | 
|-------|----------------|
| Pro (990₽) | ~100 |
| Unlimited (2490₽) | ~40 |
| Микс | 50 Pro + 20 Unlimited |

### Маркетинг план

1. **Запуск** — Product Hunt, социальные сети
2. **SEO** — статьи про AI контент, SMM
3. **Партнёрства** — интеграции с SMM-сервисами
4. **Реферальная программа** — бонусы за приглашения

## 🛠 Полезные команды

```bash
# Разработка
npm run dev

# Сборка
npm run build

# Запуск production
npm start

# Prisma
npx prisma studio     # GUI для базы данных
npx prisma db push    # Применить схему
npx prisma generate   # Сгенерировать клиент

# Stripe CLI (для тестирования webhooks)
stripe listen --forward-to localhost:3000/api/stripe/webhook
```

## 📞 Поддержка

- Email: support@socialpilot.ai
- Telegram: @socialpilot_support

---

**Сделано с ❤️ для создателей контента**
